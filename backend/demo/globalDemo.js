const KNOWN_FRAMEWORKS = {
  Malaysia: { framework: 'KSSR Semakan 2017 / KSSM', source: 'Malaysia Ministry of Education curriculum resources', url: 'https://www.moe.gov.my/' },
  'United States': { framework: 'State standards / NGSS', source: 'Next Generation Science Standards', url: 'https://www.nextgenscience.org/' },
  'United Kingdom': { framework: 'National Curriculum for England', source: 'UK Department for Education National Curriculum', url: 'https://www.gov.uk/national-curriculum' },
  Australia: { framework: 'Australian Curriculum v9.0', source: 'Australian Curriculum', url: 'https://www.australiancurriculum.edu.au/f-10-curriculum/' },
  Singapore: { framework: 'Singapore MOE Syllabus', source: 'Singapore Ministry of Education curriculum', url: 'https://www.moe.gov.sg/primary/curriculum' },
  India: { framework: 'NCF-SE 2023 / CBSE', source: 'NCERT National Curriculum Framework', url: 'https://ncert.nic.in/' },
  International: { framework: 'IB PYP / MYP / DP', source: 'International Baccalaureate programme frameworks', url: 'https://www.ibo.org/programmes/' },
};

const escapeHtml = (value = '') => String(value).replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]));
const words = (topic) => topic.split(/\s+/).filter(Boolean).slice(0, 4);
const label = (value, fallback) => value?.trim() || fallback;

function gradeBand(year) {
  const number = Number((year || '').match(/\d+/)?.[0] || 0);
  if (/preschool|kindergarten|reception|foundation|prasekolah/i.test(year)) return 'primary';
  if (number >= 10 || /form [4-6]|tingkatan [4-6]|a.?level|stpm|ib/i.test(year)) return 'upper_secondary';
  if (number >= 7 || /form [1-3]|tingkatan [1-3]/i.test(year)) return 'lower_secondary';
  return 'primary';
}

export function buildGlobalDemo({ subject, year, topic, language, country, curriculumStandard, studentPersona }) {
  const profile = KNOWN_FRAMEWORKS[country] || null;
  const framework = curriculumStandard || profile?.framework || 'International teacher-review framework';
  const source = curriculumStandard ? 'Teacher-provided curriculum standard (review required)' : profile?.source || 'Teacher-selected country or region (review required)';
  const confidenceLevel = curriculumStandard ? 'inferred' : profile ? 'inferred' : 'general';
  const terms = [...new Set([topic, ...words(topic), `${subject} vocabulary`, `${topic} application`])].slice(0, 6);
  const concepts = [`the central ideas in ${topic}`, `${subject} vocabulary`, 'evidence and examples', 'application to a familiar context'];
  const context = `In ${subject}, ${topic} should be explored as a connected set of ideas: learners first notice the important vocabulary, then explain how examples support the idea, and finally apply that understanding to a familiar situation.`;
  const objectives = [
    `Identify key ideas and vocabulary connected to ${topic}.`,
    `Explain one example or representation of ${topic} using ${subject} language.`,
    `Apply understanding of ${topic} to a teacher-selected local context.`,
  ];
  const questions = Array.from({ length: 8 }, (_, index) => {
    const term = terms[index % terms.length];
    const correct = index % 2 ? `An example connected to ${topic}` : `A key idea from ${topic}`;
    return { id: index + 1, question: `Which response best supports learning about ${term}?`, options: [correct, 'An unrelated idea', 'A copied answer without explanation', 'No evidence or example'], correct, explanation: `${correct} is strongest because it links the lesson focus to a relevant example. Ask learners to explain why their example fits ${term}, then check it against teacher-approved material.` };
  });
  const pairs = terms.map((term, index) => ({ id: index + 1, term, definition: `Explain ${term} in learner-friendly language, then connect it to one concrete example. Check that the example genuinely supports ${topic}; replace this prompt with a teacher-approved definition in ${language}.` }));
  const worksheetItems = terms.map((term, index) => ({ id: index + 1, question: `Write, draw, or explain one accurate example of ${term}. Then add one sentence explaining how your example helps someone understand ${topic}.`, answer: `Teacher review: accept an evidence-based example connected to ${topic}, plus a clear explanation of the connection.`, hint: `Use the lesson notes, underline the key vocabulary, and check that your example fits the selected ${framework} context.` }));
  const safeTopic = escapeHtml(topic);
  const worksheet = `<div class="worksheet"><h2>${safeTopic} — Teacher-editable demo worksheet</h2><p><strong>Learning context:</strong> ${escapeHtml(context)}</p><p><strong>Framework:</strong> ${escapeHtml(framework)}. Review and adapt examples, facts, and success criteria before classroom use.</p><ol>${worksheetItems.map((item) => `<li><p>${escapeHtml(item.question)}</p><p><em>Hint:</em> ${escapeHtml(item.hint)}</p></li>`).join('')}</ol><p><strong>Reflection:</strong> What is one idea you understand better now, and what evidence helped you?</p></div>`;
  return {
    status: 'complete', model: 'deterministic-global-demo', demo_mode: true,
    source_notice: `Offline demo starter requested for ${language}. ${profile ? `${profile.framework} is named from its official framework source;` : 'No verified national framework is claimed;'} the deterministic teaching template is in English and requires teacher adaptation or translation before use.`,
    lesson: {
      objectives, key_concepts: concepts, misconceptions: [`Treating ${topic} as a list of facts without explaining evidence.`, 'Using a familiar example that does not match the key concept.'], difficulty: 'intermediate', summary: `A teacher-editable ${subject} lesson starter for ${year} on ${topic}. ${context} It uses ${framework} as context and must be reviewed against local materials.`, prerequisite_knowledge: ['Relevant prior vocabulary', 'One familiar example or experience', 'Ability to describe what they notice before drawing a conclusion'], real_world_connections: [`A local example chosen by the teacher and explained with evidence`, `A classroom discussion that compares two examples connected to ${topic}`], curriculum_alignment: curriculumStandard || `Framework-level alignment only: ${framework}`, curriculum_source: source, confidenceLevel, persona_notes: `Adapt tasks, language load, and scaffolds for ${studentPersona || 'On-Level'} learners. Start with a model, then gradually remove prompts as learners explain their own examples.`, grade_band: gradeBand(year), experience_design: { primary_activity: 'quiz', secondary_activity: 'matching', why_primary_activity: 'Use the quiz as a teacher-reviewed retrieval check after modelling one example.', why_quiz: 'It surfaces vocabulary and concept gaps and gives a short explanation for discussion.', why_matching: 'It helps connect terms with teacher-approved definitions and examples before independent work.', rationale: 'This deterministic demo provides an editable starting structure rather than claiming generated subject facts. The sequence moves from vocabulary, to explanation, to application and reflection.', quiz_focus: `Key terms, evidence, and examples for ${topic}`, matching_focus: `Vocabulary and explanation links for ${topic}`, difficulty_calibration: `Teacher-adjusted for ${year}; reduce vocabulary load first, then increase the independence of explanations.`, engagement_strategy: 'Use local examples selected by the teacher and ask learners to explain why each one belongs.', confidence_score: 60 },
    },
    resources: { quiz: { data: { type: 'mcq', title: `${topic} — Review Quiz`, questions }, engineConfig: { title: `${topic} — Review Quiz`, questions, type: 'mcq' } }, matching: { data: { type: 'drag_drop', title: `${topic} — Vocabulary Match`, pairs }, engineConfig: { title: `${topic} — Vocabulary Match`, instruction: 'Match each term to its teacher-reviewed explanation. After each match, ask learners to say how the example connects to the lesson topic.', pairs, type: 'drag_drop' } }, worksheet: { html: worksheet }, answer_key: { html: `<div class="answer-key"><h2>${safeTopic} — Teacher review notes</h2><p><strong>What to look for:</strong> an accurate example, relevant vocabulary, and a sentence that explains the connection to ${safeTopic}.</p><p><strong>Feedback prompt:</strong> “What evidence makes you confident this example fits?” Use teacher-approved examples and the selected framework before marking student work.</p></div>` } },
    teaching_insights: { misconceptions: [{ mistake: `Learners may repeat words about ${topic} without explaining their meaning.`, correction: 'Ask for an example, evidence, or representation, then ask how it connects to the central idea.', example: 'Model one teacher-approved local example, think aloud about the evidence, then invite learners to improve the explanation.' }], teaching_tips: ['Open with a familiar example, then explicitly name the vocabulary and the learning goal.', 'Model a complete explanation: claim, example, and why the example fits.', 'Pause after each quiz item to discuss the explanation, not just the correct option.', 'Offer visual, spoken, and written response options, then ask every learner to justify one choice.'], daily_examples: [`A local example selected by the teacher, with a short explanation of why it fits`, `A familiar situation connected to ${topic} that learners can compare with a contrasting example`], intervention_strategy: 'Pre-teach two or three essential words, use a worked example, and provide a sentence frame: “This example connects to the topic because …”.', extension_activity: 'Ask learners to compare two examples, identify the stronger evidence, and justify their conclusion using precise vocabulary.', estimated_class_difficulty: 'moderate', time_estimate_minutes: 45, differentiation_note: `Provide visuals, chunked instructions, and sentence stems for support; ask advanced learners to justify evidence and challenge a weak example in ${language}.` },
    lesson_evaluation: { scores: { learning_coverage: 65, student_engagement: 68, difficulty_balance: 65, blooms_taxonomy: 60 }, overall_score: 65, grade: 'B', strengths: ['Accepts the selected country, subject, grade, topic, and language without an AI provider.', 'Clearly identifies the curriculum-confidence boundary.'], weaknesses: ['Teacher review is required for factual content and exact standard alignment.'], suggestion: 'Add a teacher-approved source or exact official standard before classroom delivery.', blooms_levels_present: ['Remember', 'Understand', 'Apply'], blooms_levels_missing: ['Evaluate', 'Create'] },
    study_materials: { flashcards: { title: `${topic} — Teacher-review flashcards`, cards: terms.map((term, index) => ({ id: index + 1, front: term, back: `Explain ${term} in learner-friendly language. Add one accurate example and one sentence explaining why that example supports ${topic}. Review the final wording in ${language}.`, curriculum_code: curriculumStandard || framework, emoji: '📌' })) }, study_notes: { title: `${topic} — Lesson notes`, subtitle: `${year} · ${subject}`, grade_band_label: year, sections: [{ heading: 'Why this matters', content: context, key_points: objectives }, { heading: 'How to reason about the topic', content: `A strong response does more than name a fact. It uses the key vocabulary, gives an accurate example, and explains the connection between the example and ${topic}.`, key_points: ['Name the key idea.', 'Choose relevant evidence or an example.', 'Explain the connection in a full sentence.'] }, { heading: 'Curriculum and teacher check', content: `Framework context: ${framework}. ${source}. Confirm the exact standard, local facts, and accessible language before classroom delivery.`, key_points: ['Confirm the official standard code.', 'Adapt examples for the class and locality.', 'Review language accessibility and misconceptions.'] }] }, mock_exam: { title: `${topic} — Teacher-editable practice`, exam_board: source, format_style: 'Teacher-editable curriculum practice', duration_minutes: 30, total_marks: 20, grade_band: year, questions: worksheetItems.slice(0, 4).map((item) => ({ ...item, section: 'Teacher review', marks: 5, expected_answer: item.answer, rubric: '1 mark for relevant vocabulary; 2 marks for an accurate example; 2 marks for a clear explanation of how the example connects to the topic. Adapt this rubric after checking the official curriculum.', question_type: 'short_answer' })) }, cloze_passage: { title: `${topic} — Vocabulary review`, passage: `In this lesson, learners explore ___(1)___ in ${subject}. A strong explanation uses accurate vocabulary, gives an ___(2)___, and explains the ___(3)___ between the evidence and the main idea.`, blanks: [{ id: 1, word: topic, hint: 'The selected lesson topic' }, { id: 2, word: 'example', hint: 'A concrete case that makes an idea clearer' }, { id: 3, word: 'connection', hint: 'How two ideas relate' }] }, study_map: { title: `Learning pathway: ${topic}`, description: `Move from vocabulary to evidence, explanation, and application. Confirm each branch against teacher-approved material.`, root: { id: 'root', label: topic, type: 'root', children: concepts.slice(0, 3).map((concept, index) => ({ id: `topic-${index + 1}`, label: concept, description: `Teacher-review focus for ${concept}: define it, find an example, and explain the link to the main topic.`, learning_goal: `Explain ${concept} with a relevant example and evidence.`, example: 'Use a locally appropriate example approved by the teacher.', check_question: `What evidence supports ${concept}, and why is it relevant?`, type: 'topic', children: [] })) } }, study_schedule: { title: `${topic} — 4-step study plan`, persona: studentPersona || 'On-Level', total_weeks: 1, total_lessons: 4, overview: 'Each lesson moves from understanding terms to using evidence and explaining reasoning. Review all factual content with teacher-approved material first.', schedule: ['Vocabulary and prior knowledge', 'Teacher model with evidence', 'Guided explanation practice', 'Independent application and reflection'].map((activity, index) => ({ time: `Lesson ${index + 1}`, activity, resource: index < 2 ? 'Study notes and flashcards' : 'Quiz, worksheet, and reflection', duration_minutes: 30, persona_note: index === 0 ? 'Activate what learners already know and correct vocabulary gently.' : index === 1 ? 'Think aloud: show how an example becomes an explanation.' : index === 2 ? 'Use sentence stems, then fade them as learners become confident.' : 'Ask for a justified conclusion and one next question.' })), differentiation: { beginner: 'Use visuals, a reduced vocabulary set, and sentence stems; rehearse one model together.', on_level: 'Explain one example using evidence and topic vocabulary.', gifted: 'Compare two examples, identify stronger evidence, and justify a conclusion.', sen_support: 'Chunk tasks, offer spoken response options, and use a visual claim–evidence–explanation frame.' } } },
    analytics: { generation_time_ms: 0, agent_calls: 0, resources_created: 10, estimated_time_saved_minutes: 60, model: 'deterministic-global-demo', source_confidence: confidenceLevel },
  };
}
